import React from 'react';

const Todo = () => {

    //TODO (4) написать админку добавления товара (когда нибудь)

    //TODO (3) авторизация

    //TODO (2) мигрировать на MongoDB!?

    //TODO (1) быстрый просмотр товара в модальном окне (для этого вынести из Item содержимое в отдельный компонент)
    // (возникнут проблемы с satellite в него передаём текущий item-объект)-(решение: текущий item-объект держать в redux)

    //TODO (0) переделать satellite в слайдер плитками. выбор плитки двигает её на середину страницы (становится активен клик)
    // в <div onClick={() => onClickCard()} className="item-block__image"> itemCard

    return (
        <div>

        </div>
    );
};

export default Todo;